add the following configuration:

#TODO remove the check on image because now it's only used to check if image needs to be loaded
media_player:
  - platform: feedparser
    name: NOS Feed
    feed_url: 'http://feeds.nos.nl/nosnieuwsalgemeen'
    date_format: '%a, %b %d %I:%M %p'
    inclusions:
      - title
      - media_title
      - link
      - image

Import this component via HACS

Add the following page to dd:

### News
  - type: vertical-stack
    cards:
      - name: Nieuws
        styles:
          card:
            - padding: '2% 0px'
            - text-transform: uppercase
            - font-size: 125%
            - color: 'var(--dwains-theme-accent)'
            - border-radius: 0%
            - font-weight: 100
            - letter-spacing: 10px
            - --mdc-ripple-press-opacity: 0
            - cursor: initial
          name:
            - font-weight: 900
        type: 'custom:button-card'
      - type: 'custom:auto-entities'
        filter:
          include:
            - entity_id: media_player.nos_feed
            #- entity_id: media_player.televi*
              options:
                type: 'custom:mini-media-player'
                icon: mdi:newspaper-variant
                artwork: cover
                info: scroll
                # See if possible to make On Click follow link 
                #tap_action:
                #   action: url
                #   url: link
                hide:
                   power: true
                   prev: false
                   next: false
                   play_pause: true
                   play_stop: true

                #   volume: true
                #   power_state: true
        card:
          type: entities




